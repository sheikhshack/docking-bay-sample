version: 2.1
orbs:
  docker: circleci/docker@1.0.0
  heroku: circleci/heroku@1.2.2

workflows:
  deploy:
    jobs:
      - docker/publish:
          image: $DOCKER_LOGIN/$CIRCLE_PROJECT_REPONAME
          tag: 'latest'
      - heroku/push-docker-image:
          api-key: $HEROKU_APIKEY
          app-name: $HEROKU_APPNAME
#          environment:
##            SECRET: $APP_SECRET
##            MONGODB_URI: $APP_MONGOURL
##            TEST_MONGODB_URI: $APP_TESTMONGOURL


